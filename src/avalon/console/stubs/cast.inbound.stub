"""{{ class }} attribute cast."""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any

from avalon.orm import CastsInboundAttributes


class {{ class }}(CastsInboundAttributes):
    """A write-only cast: ``casts = {"column": {{ class }}}``.

    Reads pass the stored value through untouched, which is what an
    irreversible transform needs.
    """

    def set(self, model: Any, key: str, value: Any, attributes: Mapping[str, Any]) -> Any:
        """Transform the assigned value on write, or return a mapping of columns."""
        return value
