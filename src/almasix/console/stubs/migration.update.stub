"""Alter the {{ table }} table."""

from __future__ import annotations

from almasix.orm import Blueprint, Migration, Schema


class {{ class }}(Migration):
    """{{ class }}."""

    async def up(self) -> None:
        def define(table: Blueprint) -> None:
            pass  # e.g. table.string("slug")

        await Schema.table("{{ table }}", define)

    async def down(self) -> None:
        def revert(table: Blueprint) -> None:
            pass  # e.g. table.drop_column("slug")

        await Schema.table("{{ table }}", revert)
