"""{{ class }}."""

from __future__ import annotations

from almasix.console import Command


class {{ class }}(Command):
    signature = "{{ command }}"
    description = "{{ class }}"

    def handle(self) -> int:
        self.info("{{ class }} running")
        return 0
