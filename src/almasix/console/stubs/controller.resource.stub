"""{{ class }}."""

from __future__ import annotations

from almasix.http import Controller, Request


class {{ class }}(Controller):
    """The seven actions `Route.resource` registers."""

    async def index(self):
        """GET /resource — a listing."""

    async def create(self):
        """GET /resource/create — the form that stores one."""

    async def store(self, request: Request):
        """POST /resource — persist a new one."""

    async def show(self, id: str):
        """GET /resource/{id} — one of them."""

    async def edit(self, id: str):
        """GET /resource/{id}/edit — the form that updates one."""

    async def update(self, request: Request, id: str):
        """PUT|PATCH /resource/{id} — persist a change."""

    async def destroy(self, id: str):
        """DELETE /resource/{id} — remove one."""
