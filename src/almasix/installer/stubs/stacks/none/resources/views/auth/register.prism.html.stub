@extends('layouts.minimal')

@section('title', 'Sign up — {{ app_display }}')

@section('content')
  <div class="wrap auth-panel">
    <h1>Create your {{ name }} account</h1>
    <p class="lede">One form — then you are signed in and ready to build.</p>
    @isset(error)
      <p class="flash flash-error" role="alert">{{ error }}</p>
    @endisset
    <form method="post" action="{{ form_action }}">
      @csrf
      <label>Name<input type="text" name="name" required autocomplete="name"/></label>
      <label>Email<input type="email" name="email" required autocomplete="username"/></label>
      <label>Password<input type="password" name="password" required autocomplete="new-password"/></label>
      <label>Confirm password<input type="password" name="password_confirmation" required autocomplete="new-password"/></label>
      <button type="submit" class="btn" style="width:100%">Create account</button>
    </form>
    <p style="text-align:center;color:var(--muted);font-size:0.9rem;margin-top:1.5rem">
      Already registered? <a href="{{ login_url }}">Log in</a>
    </p>
  </div>
@endsection
