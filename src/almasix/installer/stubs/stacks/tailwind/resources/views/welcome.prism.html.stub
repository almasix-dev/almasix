@extends('layouts.minimal')

@section('title', '{{ app_display }}')

@section('content')
  <section class="relative overflow-hidden">
    <div class="pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_top,_#ff7a4d22,_transparent_55%)]"></div>
    <div class="relative mx-auto max-w-5xl px-6 pb-20 pt-16 sm:pt-24">
      @isset(status)
        <p class="mb-6 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800" role="status">{{ status }}</p>
      @endisset
      <p class="text-sm font-semibold uppercase tracking-wider text-brand">{{ name }}</p>
      <h1 class="mt-3 max-w-2xl text-4xl font-semibold tracking-tight text-ink sm:text-5xl">
        Build something remarkable.
      </h1>
      <p class="mt-4 max-w-xl text-lg text-muted">
        {{ name }} is running in {{ env }}. Routing, auth, Articulate, and Prism
        are ready — start with an account or explore the health endpoint.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        @guest
          <a href="{{ register_url }}" class="rounded-lg bg-brand px-5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-brand-soft">Get started</a>
          <a href="{{ login_url }}" class="rounded-lg border border-slate-300 bg-white px-5 py-2.5 text-sm font-semibold text-ink hover:border-brand hover:text-brand">Log in</a>
        @else
          <a href="{{ dashboard_url }}" class="rounded-lg bg-brand px-5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-brand-soft">Open dashboard</a>
        @endguest
        <a href="{{ health_url }}" class="rounded-lg px-5 py-2.5 text-sm font-medium text-muted underline-offset-4 hover:text-ink hover:underline">{{ health_url }}</a>
      </div>
      <div class="mt-16 grid gap-6 sm:grid-cols-3">
        <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
          <h2 class="font-semibold text-ink">Session auth</h2>
          <p class="mt-2 text-sm text-muted">Login and registration with CSRF protection, ready for your users table.</p>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
          <h2 class="font-semibold text-ink">Articulate ORM</h2>
          <p class="mt-2 text-sm text-muted">Models, migrations, and relationships — async on SQLAlchemy Core.</p>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
          <h2 class="font-semibold text-ink">Prism views</h2>
          <p class="mt-2 text-sm text-muted">Layouts, components, and Blade-familiar directives for Python.</p>
        </div>
      </div>
    </div>
  </section>
@endsection
