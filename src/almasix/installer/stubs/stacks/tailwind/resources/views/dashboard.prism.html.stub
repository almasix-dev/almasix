@extends('layouts.app')

@section('title', 'Dashboard — {{ app_display }}')

@section('content')
  @isset(status)
    <p class="mb-6 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800" role="status">{{ status }}</p>
  @endisset
  <h1 class="text-3xl font-semibold tracking-tight">Hello, {{ user_name }}</h1>
  <p class="mt-2 text-muted">You are signed in to {{ name }}. This page uses the app layout.</p>
  <div class="mt-8 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <h2 class="font-semibold">Next steps</h2>
    <ul class="mt-3 list-disc space-y-1 pl-5 text-sm text-muted">
      <li>Edit <code class="text-ink">resources/views/</code> and <code class="text-ink">routes/web.py</code></li>
      <li>Run <code class="text-ink">python smith make:model</code> for your domain</li>
      <li>Read the docs at your Almasix documentation site</li>
    </ul>
  </div>
@endsection
