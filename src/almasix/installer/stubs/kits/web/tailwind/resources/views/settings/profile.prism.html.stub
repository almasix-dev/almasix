@extends('layouts.app')

@section('title', 'Profile — {{ app_display }}')

@section('content')

  <nav class="mb-8 flex flex-wrap gap-2 text-sm font-medium">
    <a href="/user/profile" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Profile</a>
    <a href="/user/password" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Password</a>
    <a href="/user/two-factor-authentication" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Two-factor</a>
    <a href="/user/appearance" class="rounded-lg px-3 py-1.5 hover:bg-surface border border-transparent hover:border-line">Appearance</a>
  </nav>

  <h1 class="font-display text-2xl font-semibold tracking-tight">Profile</h1>
  <p class="mt-1 text-sm text-muted">Update your name, email, and photo.</p>
  @isset(status)
    <p class="mt-4 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-800" role="status">{{ status }}</p>
  @endisset
  @isset(error)
    <p class="mt-4 rounded-lg border border-red-200 bg-red-50 px-3 py-2 text-sm text-red-800" role="alert">{{ error }}</p>
  @endisset

  <form method="post" action="{{ photo_url_action }}" enctype="multipart/form-data" class="mt-8 flex items-center gap-4">
    @csrf
    @isset(photo_url)
      <img src="{{ photo_url }}" alt="" class="h-16 w-16 rounded-full object-cover border border-line"/>
    @else
      <div class="flex h-16 w-16 items-center justify-center rounded-full bg-brand/15 text-brand font-semibold text-xl">●</div>
    @endisset
    <div>
      <input type="file" name="photo" accept="image/*" class="text-sm"/>
      <button type="submit" class="mt-2 rounded-lg border border-line px-3 py-1.5 text-sm font-medium hover:border-brand">Upload photo</button>
    </div>
  </form>

  <form method="post" action="{{ update_url }}" class="mt-8 max-w-lg space-y-4">
    @csrf
    {!! method_field('PUT') !!}
    <label class="block text-sm font-medium">Name
      <input type="text" name="name" value="{{ user_name }}" required class="mt-1 w-full rounded-lg border border-line bg-surface px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <label class="block text-sm font-medium">Email
      <input type="email" name="email" value="{{ user_email }}" required class="mt-1 w-full rounded-lg border border-line bg-surface px-3 py-2 focus:border-brand focus:outline-none focus:ring-2 focus:ring-brand/25"/>
    </label>
    <button type="submit" class="rounded-lg bg-brand px-4 py-2 text-sm font-semibold text-white hover:bg-brand-glow">Save</button>
  </form>

  <div class="mt-12 max-w-lg rounded-xl border border-red-200 bg-red-50/50 p-5 dark:border-red-900 dark:bg-red-950/30">
    <h2 class="font-semibold text-red-700 dark:text-red-300">Delete account</h2>
    <p class="mt-1 text-sm text-muted">Permanently remove your account and data.</p>
    <form method="post" action="{{ delete_url }}" class="mt-4 space-y-3">
      @csrf
      {!! method_field('DELETE') !!}
      <input type="password" name="password" required placeholder="Confirm with password" class="w-full rounded-lg border border-line bg-surface px-3 py-2 text-sm"/>
      <button type="submit" class="rounded-lg bg-red-600 px-4 py-2 text-sm font-semibold text-white hover:bg-red-700">Delete account</button>
    </form>
  </div>
@endsection
